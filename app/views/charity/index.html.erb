<h1><%= @title %></h1>

<% @charities.each do |charity| %>

  <div id="charity-<%= charity.name %>">
  <h3> Name: <%= charity.name %></h3>
  <img src="<%= charity.logo.to_s %>" alt="<%= charity.name %>" width="100"/>
  <% end %>
  </div>
